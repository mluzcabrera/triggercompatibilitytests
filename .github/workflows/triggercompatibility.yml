name: triggercompatibility

on: 
  workflow_dispatch:
    inputs:
      logLevel:
        required: true
        type: boolean
      logLevelS:
        required: true
        type: string
      
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Print output
      run: echo "Hello world logLevelS!  ${{ inputs.logLevelS }}" + ${{ inputs.logLevel }}
