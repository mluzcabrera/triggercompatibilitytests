name: triggercompatibility

on: 
  workflow_dispatch:
    inputs:
      logLevel:
        required: true
  push:
  schedule:
    - cron: "13 13 13 12 *"
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Print output
      run: echo "Hello world triggersingle!"
